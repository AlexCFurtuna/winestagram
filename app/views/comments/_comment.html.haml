%li
  = comment.body
  %small.col-md-12
    Submitted #{time_ago_in_words(comment.created_at)} ago
  = form_for [@pic, comment, Comment.new] do |f|
    = f.text_area :body, placeholder: "Add a Reply", class: "form-group"
    %br/
    = f.submit "Reply", class: "btn btn-warning"
  %ul
    = render partial: 'comments/comment', collection: comment.comments
