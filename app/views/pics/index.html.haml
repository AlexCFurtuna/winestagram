.col-md-12
  #pics.transitions-enabled
    - @pics.each do |pic|
      .box.panel.panel-default
        = link_to (image_tag pic.image.url), pic
        %h5= link_to pic.title, pic
        %p.description
          = pic.description
        %p.user
          Pic by
          = pic.user.email
          = link_to like_pic_path(pic), method: :put, class: "btn btn-default" do
            %span.glyphicon.glyphicon-thumbs-up
            = pic.get_upvotes.size
        %p
        %small
        Submitted #{time_ago_in_words(pic.created_at)} ago
